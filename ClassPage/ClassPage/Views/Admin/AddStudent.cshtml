@{
    ViewData["Title"] = "Add Student to DB";
}

<form asp-action="AddStudent" asp-controller="Admin" method="post">
    <div class="form-group">
        <label>First Name</label>
        <input type="text" class="form-control text-capitalize" name="firstName" required />
    </div>
    <div class="form-group">
        <label>Middle Name</label>
        <input type="text" class="form-control text-capitalize" name="middleName" />
    </div>
    <div class="form-group">
        <label>Last Name</label>
        <input type="text" class="form-control text-capitalize" name="lastName" required />
    </div>
    <div class="form-group">
        <label>Class</label>
        <select class="form-control" name="classId" required>
            <option disabled="disabled" selected="selected" value="">Select class</option>
            @foreach (ClassDTO c in ViewBag.ClassList)
            {
                <option value="@c.Id">@c.ClassName</option>
            }
        </select>
    </div>
    <div class="form-group">
        <label>E-mail address</label>
        <input name="email" class="form-control" type="email" required />
    </div>
    <div class="form-group">
        <label>Telephone number (without +359 or 0)</label>
        <input name="phone" class="form-control" type="tel" pattern="[0-9]{9}" />
    </div>
    <input type="submit" value="Go" class="btn btn-primary" />
</form>
